spring:
  application:
    name: ms-proveedores-catalogo
  datasource:
    url: jdbc:postgresql://localhost:5432/bd_proveedores
    username: postgres
    password: Cacaroto1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080